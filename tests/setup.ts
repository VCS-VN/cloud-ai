import { afterEach } from "vitest";

// Shared Vitest setup placeholder. Keep intentionally minimal so existing tests
// can opt into their own cleanup without hidden global side effects.
afterEach(() => {});
