# AI Storefront Builder Developer Notes

- `StorefrontSection` supports known and custom section types with common metadata.
- `AIProvider` isolates model calls from validation, persistence, and rendering.
- `OutputProvider` starts with preview URL output and can later support static export or hosted publish.
