---
layer: PRODUCT_DETAIL_PAGE
warning: >
  Route chi tiết sản phẩm — bề mặt DUY NHẤT được mutate cart. Spec hành vi rất
  chặt (model selector, gallery, bottom-sheet). Gửi nguyên văn tới model.
---
src/routes/products/$productId.tsx - createFileRoute('/products/$productId'), Route.useParams(), breadcrumb back to /products. Always destructure `const { data: product, isLoading, isError, refetch } = useProductDetail(productId)`, `const { storeDetail } = useStore()`, and cart actions from useCart() at the top. Currency from storeDetail?.setting?.currency ?? 'AUD'. Render loading skeleton during isLoading, error UI with retry on isError, and missing-product UI when `!product`; return from those branches before reading any product fields. Do NOT import { products } from @/data/products and do NOT branch on hasStoreSlug. SAFE ACCESS (STRICT): Before the `!product` guard, use only `product?.` optional chaining. Concrete safe examples: `product?.category?.name`, `product?.images?.[0]`, `product?.models?.length ?? 0`, `product?.defaultModel?.price`, `product?.descriptions ?? ''`. After the guard, nested fields still use optional chaining/nullish fallback for nullable children: `product.category?.name`, `product.images ?? []`, `product.models ?? []`, `product.defaultModel?.price`, `product.descriptions ?? ''`. Never write `product.category.name`, `product.images[0]`, `product.models.length`, or `product.defaultModel.price` without `?.`/`??`. CATEGORY EYEBROW: product.category is an object `{ id: string; name: string }`; render product.category?.name only. RENDER-LOOP GUARD (STRICT): Never use `useEffect` to copy product/query data into component state. Do NOT call `setSelectedModel`, `setSelectedImageIndex`, or any setter from an effect that depends on `product`, `product.models`, `product.images`, `images`, `models`, `selectedModel`, or derived arrays/objects. Product detail state must be event-driven only: useState is allowed for `selectedImageIndex`, `selectedModelId`, quantity, read-more, and sheet-open toggles; derive all objects from memoized arrays during render. Quantity may sync from cart only through primitive dependencies (`selectedModelId` and `cartQuantity`) or directly inside the model click handler, never from product/model objects. PRODUCT IMAGE GALLERY (REQUIRED): Compute `images` with `useMemo(() => product ? product.images ?? (product.image ? [product.image] : []) : [], [product?.images, product?.image])`; track active index via `useState(0)`; derive `safeImageIndex = Math.min(selectedImageIndex, Math.max(images.length - 1, 0))` during render; main image is `images[safeImageIndex] ?? images[0] ?? product.image`; thumbnail strip hidden when images.length <= 1. Do not reset selectedImageIndex in an effect when images changes. MODEL SELECTOR + PRICE (REQUIRED): Build `modelOptions` with useMemo from `product.models ?? []` plus `product.defaultModel`; never call `.map` or `.length` on product.models directly. Track `selectedModelId` with `useState<string | null>(null)` (NOT selectedModel object state). Derive `selectedModel = modelOptions.find((m) => m.id === selectedModelId)` during render. Do NOT preselect defaultModel or models[0]; initial selectedModelId MUST be null. If product.models is empty but defaultModel exists, render defaultModel as the single selectable option but still do NOT select it automatically. Display every available model as a selectable option/chip/card so the shopper can choose one. Selecting a model MUST set selectedModelId and sync the quantity input to the existing cart quantity for that model (`const cartQuantity = getItemQuantity(model.id); setQuantity(cartQuantity > 0 ? cartQuantity : 1)`). Display price via selectedModel?.price ?? product.defaultModel?.price ?? resolveProductPrice(product) ?? 0. User MUST explicitly select a model before cart mutation can succeed. Desktop model chips active state uses selectedModelId === model.id only. DESCRIPTIONS / READ-MORE (REQUIRED): product.descriptions is an HTML string. Import DOMPurify and useMemo, compute `sanitizedDescriptions` with the SSR guard `typeof window !== 'undefined' ? DOMPurify.sanitize(product.descriptions ?? '') : escapeHtml(product.descriptions ?? '')`, and render through `dangerouslySetInnerHTML={{ __html: sanitizedDescriptions }}` on a wrapping <div> (NOT <p>). Never render `{product.descriptions}` as JSX text and never pass unsanitized product descriptions to dangerouslySetInnerHTML. Define `DESCRIPTION_THRESHOLD = 240`; compute a plain-text length from `product.descriptions ?? ''` with HTML tags stripped; when it exceeds the threshold, render a Read more / Read less toggle. Collapsed state uses a line clamp (e.g. line-clamp-4) on the sanitized description container; expanded state removes the clamp. CART ACTIONS (REQUIRED): Product detail MUST render add/update cart controls and MUST NOT be left as a read-only product detail page. Import `toast` from `sonner`. Quantity control MUST match the page design tokens: render a compact stepper with a minus button on the left, a centered numeric quantity input/display in the middle, and a plus button on the right. Do NOT render a bare browser number input as the only quantity UI. Minus decrements by 1 and clamps at 0; plus increments by 1; manual input accepts digits only and clamps to 0+. Use getItemQuantity(selectedModel.id) only after `selectedModel?.id` exists; derive `existingQuantity = selectedModel?.id ? getItemQuantity(selectedModel.id) : 0`. If the selected model already exists in cart, the quantity input MUST show that cart quantity when the model is selected, button label is Update cart, and updateItemQuantity(selectedModel.id, quantity) sets the cart quantity. If it does not exist, button label is Add to cart and addItem(payload) creates the cart item. The cart payload MUST be built explicitly as `const payload = { product, model: selectedModel, quantity }` and passed to addItem(payload). If the user clicks Add/Update/Confirm without selectedModel, call `toast.error('Please choose a product option first')` (or localized equivalent) and do not mutate cart. Do NOT disable the primary cart button solely because selectedModel is missing; allow the click so the toast can explain the problem. Quantity 0 removes the existing item; when the item is not yet in cart, quantity 0 must not create a cart item. After a valid add/update/remove, call `toast.success(...)` to confirm the cart changed. Product detail is the ONLY cart mutation surface. MOBILE BOTTOM SHEET (REQUIRED): Mobile trigger may open before model selection so user can choose a model; Confirm button inside sheet follows the same selected-model validation and toast.error behavior as desktop. Selecting a model updates displayed price and quantity. NEVER render mobile sheet trigger above md; NEVER render desktop chips below md. The page main MUST reserve enough bottom padding on mobile so content is not occluded by the fixed bottom-sheet trigger; remove this reserve at md+ where the trigger is hidden.
