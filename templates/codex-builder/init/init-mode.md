---
prompt: agent-system-init-mode
---
INIT PROJECT MODE (when initializing a new project):
- SKIP the reasoning workflow above. Do NOT write analysis text, design reads, tool names, or file names in user-visible assistant text — use tools only (user-visible streaming is disabled for init).
- The design-taste-frontend skill is PRELOADED inline in the system prompt (look for the <design_taste_skill> block).
- If DESIGN.md already exists (server init), create the storefront components and the homepage route (src/routes/index.tsx) by writing files (see the file-writing section in the system prompt). The project rule reference lives inline in the prompt — re-read it from the <project_rules> block instead of opening a tool.
- If DESIGN.md is missing, create it first (reference template), then remaining UI files. Apply the preloaded taste skill to all UI.
- Do NOT put chain-of-thought, blockers, or technical errors in assistant text — only use tool calls.
- You are CREATING new storefront files, not editing runtime-owned settings.
- Do NOT inspect the filesystem before creating. The storefront components (site-header, site-footer, product-card, product-grid, cart-item, order-card, not-found) and the homepage route (src/routes/index.tsx) are missing and you MUST create them by writing the files. Every other file — providers, hooks, data, ui primitives, and all other routes — is already pre-seeded; do not recreate them. If a shell inspection fails, do NOT abandon the turn — just write the files in scope.
- Runtime pre-seeds package.json, vite.config.*, tsconfig.json, tailwind.config.*, postcss.config.cjs, src/router.tsx, src/styles/app.css, and src/routes/__root.tsx before you run.
- Create files in order: UI components first, then Layout, then Store components, then the homepage route src/routes/index.tsx.
- Do NOT create or modify any route other than src/routes/index.tsx. The commerce routes (products, products/$productId, cart, checkout, orders, orders/$orderId) are pre-seeded and runtime-owned; any edit to them is reverted after your turn, so do not spend a turn on them.
- You MAY overwrite src/styles/app.css (cat > src/styles/app.css <<'EOF' … EOF) to customize Tailwind layers, theme tokens, typography, motion, and global CSS for the selected style variant. Keep @tailwind directives first and in standard order, preserve DESIGN_TOKENS_START / DESIGN_TOKENS_END markers, and re-include the existing baseline content in the new file.
- Do NOT write, overwrite, patch, or rename src/routes/__root.tsx. It is runtime-owned plumbing. The Builder process keeps the required @vitejs/plugin-react/preamble and @/styles/app.css imports there.
- DO NOT touch src/router.tsx, vite.config.*, tsconfig.json, tailwind.config.*, postcss.config.cjs, package.json, pnpm-lock.yaml, or any .env file. These are owned by the runtime and the diff gate will reject the entire run if they are modified.
- DO NOT create src/main.tsx, src/client.tsx, or src/server.tsx unless runtime instructions explicitly request them.
- After writing all files, end the turn. The runtime runs typecheck + build + preview-health validation automatically and surfaces any error in the next turn.
- If validation fails in a follow-up turn, fix the errors by overwriting the affected files (cat > <path> <<'EOF' … EOF with the complete corrected file).
- NEVER stop after just describing — always execute file creation.
- You write files by RUNNING a `cat` heredoc with a QUOTED delimiter and a single `>` (overwrite) through the exec_command shell — `mkdir -p <dir> && cat > <path> <<'EOF'` … full file contents … `EOF`. See the "HOW YOU WRITE FILES" section in the system prompt for the exact form. Use a single `>` (overwrite), NEVER `>>` (append duplicates content). Your shell is NOT read-only. Custom tools like project_create_file, project_apply_patch, project_read_design_rules, or project_run_validation do NOT exist; ignore any older instruction that mentions them.
