---
layer: COMPONENT
warning: >
  Component tái dùng storefront agent phải tự viết. KHÔNG chứa shadcn primitive
  (đã sinh sẵn) và KHÔNG chứa các section trang trí thuần thẩm mỹ (hero, trust,
  feature-band, newsletter) — agent tự thiết kế chúng theo skill + DESIGN.md.
  Gửi nguyên văn tới model. Các spec ở đây là HÀNH VI, không phải layout cứng.
  CSS/spacing/shape/color literal đã được strip — agent tự quyết định visual
  bằng dial values và tokens trong DESIGN.md.
---
COMPONENT CONTRACTS:

Cross-cutting rules referenced by every component below:
{{include:canonical/brand-name.md}}
{{include:canonical/ui-tokens.md}}
{{include:canonical/cart-mutation.md}}
{{include:canonical/data-shape.md}}

Per-component contracts:

src/components/layout/site-header.tsx - REQUIRED if missing. Import { ThemeToggle } from '@/components/layout/theme-toggle'. Retail header pinned to the top of the viewport. Destructure `const { storeDetail } = useStore()` and `const { totalItems } = useCart()` at the top, then render brand name as <Link to='/'>{storeDetail?.name}</Link>, a search bar, <ThemeToggle /> before the cart button, and a ShoppingCart icon Button linking to /cart with a cart badge that appears only when totalItems > 0. Derive storeId from storeDetail?.id (NOT from useStoreDetail().data?.id). Do NOT use websiteConfig.store.name, do NOT call useStoreDetail() directly here, do NOT hardcode the brand string. NO Home/Products/Orders nav links. NO mobile Sheet menu. Search-bar accent colors MUST bind to the DESIGN.md primary token (input fill, focus ring, submit button background, match highlight) — do NOT hardcode rose/pink/slate. Search input placeholder MUST be exactly 'What are you looking for?'. Submit (form onSubmit OR Enter) calls useNavigate() from @tanstack/react-router and navigate({ to: '/products', search: { q: value.trim() } }) when value is non-empty, then closes the dropdown. Debounce the suggestions input value by 800ms via a useEffect+setTimeout — track BOTH the raw input value (for the input field, dropdown visibility gating, and form submit) AND a debouncedValue (passed as the query to useProductSuggestions) so users can type freely without firing a request per keystroke. Suggestions dropdown renders below the input when the input is focused AND inputValue.trim().length > 0 AND suggestions.length > 0. The dropdown container MUST be an opaque overlay surface with `bg-popover text-popover-foreground border border-border shadow-lg` (or equivalent semantic popover/card tokens), `z-50`, and no transparency/backdrop blending. Do NOT use `bg-transparent`, `bg-background/..`, `backdrop-blur`, `mix-blend-*`, `opacity-*`, or nested conflicting background panels on the dropdown container; page/header backgrounds must never show through suggestion rows. Each suggestion row contains a leading Search icon and the suggestion text, is `bg-transparent` by default, and uses `hover:bg-accent hover:text-accent-foreground` / active `bg-accent text-accent-foreground`. Clicking a row sets the input value to the suggestion, navigates to /products with search { q: suggestion }, and closes the dropdown. Highlight matched substrings of the current input value against each suggestion (case-insensitive) by escaping regex metacharacters in the query, splitting via new RegExp('(' + escaped + ')', 'gi'), and wrapping matched parts in a highlight span tinted with `text-primary` (same weight, no background). Accessibility: input role='combobox' with aria-expanded and aria-controls='site-search-suggestions'; dropdown role='listbox' id='site-search-suggestions'; rows role='option' aria-selected. Keyboard: ArrowDown/ArrowUp move active row, Enter selects active row (or submits typed value when no row is active), Escape closes. Hide dropdown on outside click and on isError from the suggestions hook. Wire suggestions via useProductSuggestions({ storeId, query: debouncedValue }) — do NOT branch on hasStoreSlug here, the hook handles the fallback internally.
SiteHeader search suggestions MUST use the canonical state model: inputValue controls the input; debouncedValue is passed to useProductSuggestions; open closes only on outside click, Escape, submit, or suggestion click; showDropdown = open && inputValue.trim().length > 0 && suggestions.length > 0 && !isError. Render suggestions.map directly inside #site-search-suggestions. Do NOT hide the dropdown just because debouncedValue differs from inputValue or a new request is loading. Suggestion rows MUST use onMouseDown(event.preventDefault()) so blur cannot close the list before selection. Never make network suggestions without visible dropdown UI. Do not restyle shadcn Button/Input primitives with arbitrary raw background/text colors inside the search form; use their variants and semantic token classes so contrast stays stable across the generated palette.
SiteHeader MUST derive `const storeId = storeDetail?.id` from `const { storeDetail } = useStore()` and pass useProductSuggestions({ storeId, query: debouncedValue }); do NOT call useStoreDetail() inside SiteHeader.

src/components/layout/site-footer.tsx - CREATE if missing. Storefront footer with Shop/Support/Company/Connect column groups in a responsive grid. Colors MUST come from DESIGN.md token utilities (e.g. deep/deep-foreground), never hardcoded hex. For social/contact links, do NOT import brand icons from lucide-react (Instagram, Facebook, Twitter/X, LinkedIn, YouTube, TikTok, etc.); use generic icons known to exist such as Mail, MessageCircle, Send, Globe, ExternalLink, MapPin, Phone, or text labels.

src/components/layout/theme-toggle.tsx - PRE-SEEDED. ThemeToggle toggles document.documentElement.classList 'dark' and localStorage key `storefront-theme`. The generated storefront defaults to light theme unless the user explicitly toggles dark. SiteHeader must import and render it.

src/components/layout/route-loading-bar.tsx - PRE-SEEDED. RouteLoadingBar uses useRouterState from @tanstack/react-router with select: state.status, visible only when status === 'pending'. Render a fixed-top progress indicator above the whole website that uses the DESIGN.md primary token (bg-primary), pointer-events-none, with opacity/transform transition, no layout shift, no minimum-duration timer, no hard reload fallback.

src/components/store/product-card.tsx - Card with product image, name, formatted price, sale Badge, Select option Button linking to /products/$productId, Heart wishlist. Product cards MUST NOT mutate cart and MUST NOT call toast for add-to-cart; only product detail can add/update cart after explicit model selection. Render image via product.image ?? product.images?.[0] inside an <img className='object-cover' /> as a BARE element (NOT wrapped in a Link), falling back to a seeded Lorem Picsum photo `https://picsum.photos/seed/${encodeURIComponent(product.id)}/600/600` (same <img>, object-cover) only when both fields are missing — never a gradient-filled div. Wrap ONLY the product title text with <Link to='/products/$productId' params={{ productId: product.id }}> — the Link contains the <h3>{product.name}</h3>, never the image. Render price via formatMoney(resolveProductPrice(product), { currency: useStore().storeDetail?.setting?.currency ?? 'AUD' }). Render the product description preview as a two-line truncated block under the title using DOMPurify-sanitized HTML: `import DOMPurify from 'dompurify'`, compute `const sanitizedDescriptions = useMemo(() => { const html = product.descriptions ?? ''; return typeof window !== 'undefined' ? DOMPurify.sanitize(html) : escapeHtml(html) }, [product.descriptions])`, and render via dangerouslySetInnerHTML on a wrapping <div> (NOT a <p> — HTML may contain block-level tags). NEVER render product.descriptions raw without DOMPurify.sanitize and the SSR guard.

src/components/store/product-grid.tsx - CREATE when needed. Title, responsive grid mapping ProductCard; useProductsList with infinite scroll sentinel pattern (see store-runtime rules). Do NOT import { products } from @/data/products.

src/components/store/cart-item.tsx - CREATE when needed. Cart row for useCart() items: checkbox, thumbnail, quantity controls, remove, line total — wire updateItemQuantity/removeItem; currency from useStore().

src/components/store/cart-drawer.tsx - PRE-SEEDED. Lightweight cart preview/drawer surface using useCart(), cart item count, and links to /cart. Patch only if a page imports or displays it.

src/components/store/order-card.tsx - Card with order#, date, status Badge, total, Link to detail.

src/components/store/not-found.tsx - Storefront Not Found component for unmatched preview paths. Must use DESIGN.md tokens/style and include valid CTAs to '/' and '/products'. User customizations are allowed; do not replace custom Not Found UI unless it is missing.

You MAY create additional storefront sections/components (hero, trust signals, category bands, newsletter, etc.) as the design calls for — design their layout yourself following the taste skill and DESIGN.md. There is no fixed section list; build what makes a compelling retail storefront for this store. Visual decisions (sizing, shape, spacing, color, shadow) are yours to make based on the dial values declared in DESIGN.md front-matter and the tokens in DESIGN.md sections 1-8 — do not reach for arbitrary Tailwind literals (e.g. `text-slate-700`, `bg-white`, `rounded-2xl`, `shadow-lg`) when DESIGN.md tokens cover the role.
